import { refToBranch } from '../../../helpers/ref-to-branch';
import { module, test } from 'qunit';

module('Unit | Helper | ref to branch');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = refToBranch(42);
  assert.ok(result);
});
